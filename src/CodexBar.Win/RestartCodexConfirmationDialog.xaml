<Window x:Class="CodexBar.Win.RestartCodexConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="重启 Codex Desktop"
        Width="440"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#FFF5F5F5"
        FontFamily="Microsoft YaHei UI"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True">
  <Window.Resources>
    <Style x:Key="ActionButton" TargetType="Button">
      <Setter Property="Height" Value="34" />
      <Setter Property="Padding" Value="14,0" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Foreground" Value="#1C1C1C" />
      <Setter Property="Background" Value="White" />
      <Setter Property="BorderBrush" Value="#D9D9D9" />
      <Setter Property="FontSize" Value="12" />
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="8">
              <ContentPresenter HorizontalAlignment="Center"
                                VerticalAlignment="Center" />
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
      <Setter Property="Foreground" Value="White" />
      <Setter Property="Background" Value="#0067C0" />
      <Setter Property="BorderBrush" Value="#0067C0" />
    </Style>

    <Style x:Key="SubtleButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
      <Setter Property="Background" Value="#FFF5F5F5" />
      <Setter Property="BorderBrush" Value="#FFF5F5F5" />
      <Setter Property="FontWeight" Value="Normal" />
    </Style>
  </Window.Resources>

  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Border Grid.Row="0"
            Padding="14"
            Background="White"
            BorderBrush="#11000000"
            BorderThickness="1"
            CornerRadius="8">
      <StackPanel>
        <TextBlock Text="重启 Codex？"
                   Foreground="#1C1C1C"
                   FontSize="16"
                   FontWeight="SemiBold" />
        <TextBlock Margin="0,10,0,0"
                   Foreground="#605E5C"
                   FontSize="12"
                   TextWrapping="Wrap"
                   Text="将关闭当前 Codex 窗口和后台进程，然后重新启动。" />
      </StackPanel>
    </Border>

    <Border Grid.Row="1"
            Margin="0,12,0,0"
            Padding="14"
            Background="#F7FAFF"
            BorderBrush="#CFE4F9"
            BorderThickness="1"
            CornerRadius="8">
      <CheckBox x:Name="DoNotAskAgainBox"
                Content="不再提示"
                VerticalAlignment="Center" />
    </Border>

    <StackPanel Grid.Row="2"
                Margin="0,14,0,0"
                Orientation="Horizontal"
                HorizontalAlignment="Right">
      <Button Content="取消"
              Width="86"
              Margin="0,0,8,0"
              Style="{StaticResource SubtleButton}"
              Click="Cancel_Click" />
      <Button Content="确认重启"
              Width="98"
              Style="{StaticResource PrimaryButton}"
              Click="Confirm_Click" />
    </StackPanel>
  </Grid>
</Window>
